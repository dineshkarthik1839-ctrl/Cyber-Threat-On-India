import type { Threat } from "../../types/threat";
const cls=(s:string)=>s.toLowerCase();
export default function ThreatTable({items}:{items:Threat[]}){return <div style={{overflowX:"auto"}}><table className="table"><thead><tr><th>Indicator</th><th>Origin</th><th>Target</th><th>Technique</th><th>Severity</th><th>Seen</th></tr></thead><tbody>{items.map(t=><tr key={t.id}><td><b style={{color:"#dcecff"}}>{t.sourceIp}</b><div className="muted" style={{fontSize:10,marginTop:3}}>{t.id}</div></td><td>{t.countryCode} · {t.sourceCountry}</td><td>{t.targetState}</td><td>{t.attackType}<div className="muted" style={{fontSize:10,marginTop:3}}>{t.mitre}</div></td><td><span className={`severity ${cls(t.severity)}`}>{t.severity}</span></td><td className="muted">{t.timestamp}</td></tr>)}</tbody></table></div>}