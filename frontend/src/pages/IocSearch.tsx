import { useState } from "react";
export default function IocSearch(){const [value,setValue]=useState("");const [searched,setSearched]=useState(false);return <div className="page"><div className="eyebrow">Investigation workspace</div><h1 style={{margin:"7px 0 5px",fontSize:25}}>IOC search</h1><p className="muted" style={{fontSize:12}}>Enrich IP addresses, domains, hashes, CVEs, and malware families across your feeds.</p><div className="panel" style={{padding:28,marginTop:24,maxWidth:900}}><label style={{fontSize:12,color:"#aebed0"}}>Indicator</label><div style={{display:"flex",gap:10,marginTop:9}}><input className="field" value={value} onChange={e=>setValue(e.target.value)} placeholder="e.g. 185.220.101.45, CVE-2025-XXXX, suspicious-domain.in"/><button className="action-button" onClick={()=>setSearched(true)}>Investigate</button></div>{searched&&<div style={{marginTop:25,padding:18,borderRadius:10,background:"#0a1524",border:"1px solid #223b57"}}><span className="severity high">High confidence</span><h2 style={{fontSize:16,margin:"13px 0 8px"}}>{value || "Indicator"} is associated with hostile activity</h2><p className="muted" style={{fontSize:12,lineHeight:1.7}}>Observed in credential-access activity targeting Indian internet-facing services. Correlates with MITRE ATT&CK T1110. Block at perimeter controls and search historical authentication logs.</p><div className="eyebrow" style={{marginTop:17,fontSize:9}}>Sources</div><p style={{fontSize:12}}>ThreatFox · AbuseIPDB · AlienVault OTX</p></div>}</div></div>}